# 人工回归清单

## 说明与准备

- [ ] 记录 Chrome 版本、扩展 `manifest.json` 版本、测试日期与操作系统；确认 `manifest.json` 与 `package.json` 版本一致，且不低于准备发布的 Git 标签。
- [ ] 执行 `npm run verify`，确认所有自动化测试和语法检查通过。
- [ ] 在 `chrome://extensions/` 使用“加载已解压的扩展程序”选择当前根目录。
- [ ] 每次重新加载扩展后刷新现有 ChatGPT 页面，确认只有一个蓝紫色“提示框 + 光标”浮动入口。
- [ ] 先用 `tests/fixtures/chatgpt-composer.html` 验证合成 DOM，再在真实 chatgpt.com 完成标为真实页面的项目；fixture 不等价于真实冒烟。

## 页面与会话类型（真实 ChatGPT）

- [ ] 新对话：composer 出现后浮钮可见、可操作且不遮挡发送按钮。
- [ ] 旧对话：加载已有消息的对话后，助手列表和插入功能正常。
- [ ] 临时对话：启用临时对话后仍能插入；扩展不会读取或保存聊天消息。
- [ ] 在新对话、旧对话和临时对话之间切换，SPA 重建前后 `#phg-root`、浮动入口和面板始终各只有一个，保持 UI singleton（单例）。

## CRUD 与占位符历史

- [ ] 新增名称、正文和占位符，保存后立即出现在列表。
- [ ] 编辑名称、正文和占位符，原记录被更新而不是重复新增。
- [ ] 拖动词条左侧手柄调整顺序，松开后列表立即按新顺序排列，刷新后仍保持。
- [ ] 细指针设备上按住词条正文拖动超过阈值后只改顺序，松开不会插入；未超过阈值的轻点仍插入。
- [ ] 触屏用手柄拖动排序，拖动词条正文仍可滚动列表，不会开始排序。
- [ ] 焦点在拖动手柄时，方向键上/下将当前词条移动一格，且不会插入。
- [ ] 顺序保存失败时列表回滚到原顺序，并出现中文错误提示。
- [ ] 删除提示词时出现确认对话框；取消不删除，确认后才删除。
- [ ] 模拟或触发保存失败，确认原列表/历史回滚，表单保留并可重试。
- [ ] 保存自定义占位符后，历史按最近优先、去重且最多 5 个。
- [ ] 点击历史值会填入表单；删除历史不影响已保存提示词。
- [ ] 刷新页面后 CRUD 结果和占位符历史仍持久化。

## 插入位置与正文

- [ ] 空输入框：插入无占位符正文，文本完整，光标位于结尾。
- [ ] 已有文本：分别把光标放在开头、中间、结尾插入，原文字不被覆盖或删除。
- [ ] 有选区时插入发生在选区开头，原选中文字仍保留。
- [ ] 插入多行正文，换行、段落顺序与光标偏移正确。
- [ ] 默认 `【光标】`：只移除第一个，光标停在其位置，后续同名标记保留。
- [ ] 旧版 `[光标]`：在没有默认标记时兼容命中。
- [ ] 自定义占位符：优先级高于正文中更早出现的 `【光标】`、`[光标]` 和历史值。
- [ ] 默认开启自动选中时，插入含 `【主题】` 的提示词，整段 `【主题】` 保持在正文中并被选中；直接输入可整体替换。
- [ ] 同一条提示词含多个 `【…】` 时，只选中第一处，后续标记保持原样。
- [ ] 同时含当前自定义光标和 `【主题】` 时，自定义光标优先移除并定位，不触发通用选区。
- [ ] 当前自定义光标未实际出现在正文时，继续选中第一处 `【…】`，不会因“配置过自定义值”而阻断。
- [ ] 在标题栏“插入设置”中关闭自动选中后，普通 `【主题】` 不再产生选区；`【光标】`、`[光标]` 和自定义光标仍保持原行为。
- [ ] 重新开启自动选中并刷新页面，设置仍然生效；另一个已打开标签页也会同步该开关。
- [ ] 历史自定义占位符：当前记录值、默认值和旧版值都未命中时仍可兼容。
- [ ] 正文没有三类已知占位符时，文本不被删减且光标到新文本末尾。
- [ ] 自定义占位符前后有中文、英文、emoji 时，不出现字符偏移或截断。

## 附件、发送与原生控件

- [ ] 输入框附近已有附件时，在附件前和附件后分别放置光标并插入；附件节点、文件名和预览保留。
- [ ] 插入后 ChatGPT 发送按钮按正常输入规则启用，但助手不自动发送。
- [ ] 手动点击发送仍由 ChatGPT 正常处理；助手不拦截真实页面发送。
- [ ] Composer 外的原生 contenteditable、textarea、搜索框和页面按钮不受影响。
- [ ] fixture 中点击发送只显示“已拦截”，不会产生网络请求。

## Quiet Orbit（静默轨道）浮动入口、面板与词条

- [ ] idle：浮动入口只显示自有的提示框轮廓、文本插入光标与指针图形，没有文字、OpenAI/Gemini 名称或品牌图形；基础阴影克制，没有常驻光晕。
- [ ] hover/focus（精细指针）：悬停出现柔和光晕、阴影加深并向上抬升 3 px；键盘 `:focus-visible` 的轮廓和光晕清楚。
- [ ] active/drag：按下时取消抬升并轻微缩小，拖拽时使用抓取反馈且位置连续；松开后的拖拽不会误切换面板，四种状态可区分。
- [ ] 连续快速打开/关闭面板至少 10 次，没有闪白、模糊残影、跳位或不可点击帧；打开约 270 ms 稳定，关闭约 175 ms 完成。
- [ ] 面板保持紧凑的 header、可滚动 body、footer 三段结构；列表很多时只滚动 body，关闭和新增入口仍可操作。
- [ ] 精细指针下，词条静止时不显示操作；只有当前词条悬停或 `:focus-within` 时才出现铅笔和垃圾桶图标，其他词条仍不显示操作。
- [ ] 点击词条正文只插入提示词；点击编辑或删除只打开相应对话框，绝不触发插入。
- [ ] 粗指针、触屏或不支持悬停时，所有词条的编辑/删除始终可见、可点击，关键触控目标至少 44 px。
- [ ] 卡片的编辑和删除显示为铅笔、垃圾桶图标；悬停提示、中文无障碍名称、键盘焦点与点击行为正确，点击图标本身不会插入提示词。

- [ ] 单击浮钮打开/关闭面板；小于拖拽阈值的轻微移动仍按单击处理。
- [ ] 拖拽浮钮到四角，按钮不会越出视口，拖拽结束位置被保存。
- [ ] 刷新页面后拖拽位置恢复。
- [ ] 清除 `ph_button_pos` 后刷新，浮钮距网页右侧、底部各约 24 px；拖到距边缘 12 px 的位置并刷新后仍恢复该保存坐标。
- [ ] 分别把浏览器缩放设为 80%、100%、125%、200%，浮钮与面板保持可见。
- [ ] 在窄窗口、低高度和旋转后的移动视口中，缩放/调整窗口会把浮钮恢复到安全范围。
- [ ] 位置保存失败时，本页安全位置保留并出现错误提示；刷新后可恢复旧持久位置。
- [ ] 加载、插入、保存和位置失败等状态提示始终锚定浮动入口：优先位于其上方，空间不足时移到下方，并完整夹取在视口内；拖拽、缩放和 SPA 重建后仍跟随新位置。

## SPA 与生命周期（真实 ChatGPT + fixture）

- [ ] 在站内切换对话/路由，composer 被替换后仍能插入。
- [ ] 连续快速切换 SPA 路由，没有重复浮钮、重复面板或异常循环；`#phg-root` 始终是 singleton（单例）。
- [ ] fixture 点击“模拟 SPA 重建输入框”，在替换节点中再次放置光标并成功插入。
- [ ] ChatGPT 暂时移除 composer 时插入显示“未找到输入框”；composer 恢复后无需刷新即可重试。
- [ ] 扩展重新加载导致旧页面 context invalidated 时，页面不会出现未捕获异常；刷新后恢复。

## 主题、动画与可访问性

- [ ] 明色（浅色）主题下文字、边框、主按钮和危险按钮对比清楚。
- [ ] 暗色主题下文字、边框、主按钮和危险按钮对比清楚。
- [ ] 浅色主题下编辑/删除图标使用浅色或淡红表面，不出现黑色按钮块；深色主题下两者仍有足够对比，切换主题后无需刷新即可更新。
- [ ] 标题栏分隔线、关闭图标、创建入口、焦点环、面板与弹窗阴影在明暗主题中均与页面一致。
- [ ] fixture 依次切换明色、暗色和系统主题，扩展变量与 ChatGPT 根主题立即同步；系统模式分别在操作系统明/暗偏好下复核。
- [ ] 系统开启减少动画（`prefers-reduced-motion: reduce`）后，入口、面板、词条、对话框和状态提示不再播放过渡动画，也没有错峰、缩放、位移或侧轨增长，状态仍可理解。
- [ ] fixture 的主题选择器和“模拟减少动画”可切换对应状态；真实页面仍需用页面主题和系统设置分别复核。
- [ ] 新增/编辑对话框的 header、可滚动 body、footer 三段都在视口内；名称、正文、占位符与历史区域可操作，窄屏或低高度下底部按钮仍可到达。
- [ ] 保存失败后对话框不关闭，名称、正文、占位符和历史仍保留，可直接重试；Esc、焦点循环及关闭后的 opener 焦点恢复正常。
- [ ] 所有主要按钮的可点击目标至少 44 px，键盘焦点轮廓可见。
- [ ] 按 Esc 关闭新增/编辑/删除对话框并把焦点恢复到原触发按钮。
- [ ] 面板无对话框时按 Esc 关闭面板并把焦点恢复到浮钮。
- [ ] 对话框内按 Tab 与 Shift+Tab，焦点只在可用控件间循环且首尾衔接。
- [ ] 插入成功关闭面板后，焦点恢复到 ChatGPT 输入框中的预期光标，而不是浮钮。
- [ ] 点击面板外关闭面板，不意外激活或修改页面原生控件。

## 存储与跨标签

- [ ] 两个 ChatGPT 标签页同时打开：在 A 新增/编辑/删除/调整顺序后，B 自动同步列表与历史。
- [ ] A 正在保存时 B 写入，A 在本次写入结束后重新加载外部状态，不停在 busy。
- [ ] 清除 `ph_prompts`、`ph_placeholder_history`、`ph_button_pos`、`ph_auto_select_bracket_placeholder` 后刷新，恢复安全空列表、默认位置和默认开启的自动选中。
- [ ] 损坏记录、重复历史和非法坐标不会让 UI 崩溃；无效项被安全过滤。
- [ ] 模拟 3000 ms 存储超时，错误可见，候选状态不被假装提交。

## 权限与隐私边界

- [ ] `manifest.json` 声明 `storage` 权限和 `https://api.github.com/*` 主机权限，页面匹配范围仍为 `https://chatgpt.com/*`，没有后台/service worker 或远端脚本。
- [ ] 完成打开、CRUD、插入、拖拽、主题与 SPA 流程（不点检查更新）后，扩展没有发起网络请求、没有读取或保存 API Key，也没有上传提示词或聊天内容。
- [ ] 插入设置中显示的“当前版本”与 `chrome://extensions/` / `manifest.json` 完全一致；点击「检查更新」后，已是最新、发现新版本（出现 GitHub 按钮和重新加载说明）、失败时均可理解；不点该按钮则不访问 GitHub。
- [ ] 插入只更新输入框并触发宿主所需输入事件；发送按钮可以按 ChatGPT 规则启用，但扩展不自动发送消息。

## 控制台与网络检查

- [ ] 打开 DevTools 控制台，完成 CRUD、插入、拖拽、SPA、Esc/Tab 流程后没有未捕获异常或持续警告。
- [ ] 打开 Network 面板并清空记录，不点「检查更新」时操作助手不会产生由扩展发起的网络请求；点击后仅出现对 GitHub API 的请求。
- [ ] Sources/Application 中确认没有 background/service worker、模型脚本、远端代码或 API Key 存储。
- [ ] Application → Extension storage 中只有三个预期键，内容与当前 UI 一致。

## 结果记录

- [ ] 记录每个失败项目的页面类型、复现步骤、控制台信息和 DOM 截图。
- [ ] 若 ChatGPT DOM 已变化，先补失败自动化测试与 fixture，再修改生产选择器。
- [ ] 所有真实页面项目通过后，记录最终日期、浏览器版本和测试人。
